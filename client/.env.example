# URL completa del backend (sin barra final)
# En desarrollo: dejar vacío (Vite proxy maneja /api → localhost:3000)
# En producción (Netlify): poner la URL de Render/Railway
VITE_API_URL=https://tu-backend.onrender.com

# Google OAuth — mismo Client ID que en el backend
VITE_GOOGLE_CLIENT_ID=tu-google-client-id.apps.googleusercontent.com
